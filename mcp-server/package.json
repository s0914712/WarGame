{
  "name": "wargame-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the Taiwan Wargame sim engine for LLM-driven play + benchmark",
  "scripts": {
    "start": "tsx server.ts",
    "test:tools": "tsx server.ts --self-test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "~5.7.0"
  }
}
